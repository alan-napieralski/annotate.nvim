# Tests

This directory is reserved for automated tests for `annotate.nvim`.

The most valuable first test targets are:

- project root resolution with and without `neovim-project`
- JSON store parsing and write roundtrips
- annotation list parsing and save behavior
- export formatting for AI handoff
